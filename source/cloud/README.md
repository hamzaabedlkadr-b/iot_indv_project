# Cloud Integration

This folder tracks the `LoRaWAN + TTN` communication path.

Current state:

- the firmware now prepares compact aggregate uplink payloads from real analysis windows,
- local development keeps radio transmission disabled until campus testing,
- the remaining Phase 9 work is to connect the board to real `TTN` coverage and capture proof of uplink.

Planned campus-test contents:

- `TTN` setup notes
- payload format documentation
- decoder examples
- screenshots or logs proving successful uplinks
