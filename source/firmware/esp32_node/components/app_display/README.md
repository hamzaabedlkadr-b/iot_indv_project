# OLED Display

This module is currently a serial-status placeholder rather than a full OLED UI.

It currently logs:

- connection and event-group state
- current sampling frequency
- dominant frequency and latest aggregate value
- enough live status to explain the pipeline even if the physical OLED is not used
